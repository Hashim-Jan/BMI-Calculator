import 'package:flutter/material.dart';

class GenderResult extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text("Welcome to 2nd screen")),
      body: Center(child: Text("maze",style: TextStyle(color: Colors.white),)),
    );
  }
}
